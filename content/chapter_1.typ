#import "util.typ": *

#baeknote-template(1000, "문제 제목")[
    == 문제 내용
    
    === 입력
    
    $N$의 값이 주어집니다. ($1 <= N <= 100 space.thin 000$)
    
    === 출력
    
    문제의 정답을 출력합니다.
    
    == 문제 풀이
    
    #spoiler[
        #baeknote(1428) 풀이는 스포일러입니다.
    
        $ x + y = a \ y + z = b \ z + x = c $
    
        Euler characteristic을 이용한 풀이가 가능합니다.
    ]
]
